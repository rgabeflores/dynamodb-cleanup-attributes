# DynamoDB Configuration
DYNAMODB_TABLE_NAME=your-table-name
DYNAMODB_TABLE_NAME=your-partition-key
ATTRIBUTES_TO_REMOVE=unusedAttribute1,unusedAttribute2,obsoleteField,deprecatedProperty

# AWS Configuration
AWS_REGION=us-east-1
# Note: AWS credentials should be configured via AWS CLI, IAM roles, or environment variables
# AWS_ACCESS_KEY_ID=your-access-key (optional - use AWS CLI or IAM roles instead)
# AWS_SECRET_ACCESS_KEY=your-secret-key (optional - use AWS CLI or IAM roles instead)

# Performance Tuning (Optional - defaults will be used if not specified)
BATCH_SIZE=25
MAX_RETRIES=5
INITIAL_DELAY=100
